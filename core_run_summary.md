# AI agent-1 核心运行总结

## 目标

做一个本地 AI agent，满足三个核心能力：

- 读写工作目录文件
- 多轮文本交互
- 通过前端直接操作和查看结果

## 这次跑通的关键点

1. 先把项目独立到 `AI agent-1` 目录，避免和原始仿真文件混在一起。
2. 后端用 Python 处理文件读写、状态保存和示例总结。
3. 前端用 Node 启动静态页面，并代理到 Python 后端。
4. 反复修复了这些问题：
   - Python 模块导入路径
   - Node 前端和 Python 后端的启动顺序
   - PowerShell 启动方式和后台进程管理
   - 多轮对话里“继续”指令的上下文命中

## 使用经验

- 文件读写最好限制在 workspace 根目录内，避免越界。
- 文本交互要保留历史和记忆字段，才能支持多轮追问。
- 前端最好把“聊天”“读文件”“写文件”“生成总结”拆成清晰入口。
- 外部大模型接入要单独暴露 API，不要和本地规则逻辑混在一起。

## 当前能力

- 可列出、读取、写入 `.txt` 文件
- 可生成工作目录文本总结
- 可维持多轮对话上下文
- 可通过独立 API 接入大模型

