# 下一次开发 Agent 的工作手册

这份文档记录本次 AI Agent 项目开发中遇到的主要问题、修复方式，以及下一次开发时应遵守的工作原则。

## 1. 项目目标回顾

- 后端：Python + FastAPI
- 前端：Node.js + Express + 原生 HTML/CSS/JS
- 能力：多轮对话、文件读取、文件总结、工具调用、跨域访问
- 模型：通过 `.env` 或环境变量切换 OpenAI / DeepSeek 等 OpenAI 兼容服务

## 2. 本次开发中遇到的问题

### 2.1 Node.js 未全局可用

问题：
- 系统里最初没有 `node` / `npm`
- 项目可跑，但整个 Codex 环境无法直接调用 Node

解决：
- 使用 `winget` 安装 Node.js LTS
- 补全 PATH，并确保当前会话能识别 `node` / `npm`

经验：
- 先验证系统级命令是否可用，再做项目级安装

### 2.2 后端端口占用，导致 `uvicorn` 启动失败

问题：
- `uvicorn main:app --reload --port 8000` 报 `WinError 10013`
- 实际原因是旧的后端进程还占用着 8000 端口

解决：
- 用 `netstat` / `Get-NetTCPConnection` 定位占用进程
- 停掉旧进程后再启动

经验：
- 启动失败时先查端口，不要默认是代码错误

### 2.3 `.env` 写了 API key，但后端没读取

问题：
- 用户把 `API key` 放进 `.env`
- 代码只读 `os.getenv()`，没有自动加载 `.env`

解决：
- 在 `config.py` 中加入 `python-dotenv`
- 启动时自动 `load_dotenv()`

经验：
- 如果项目要求支持 `.env`，必须显式加载，不能假设外部已经导出环境变量

### 2.4 DeepSeek 接入配置不统一

问题：
- `.env` 里写了 DeepSeek 的地址，但早期代码仍按 OpenAI 默认值走
- base URL 需要统一规范化

解决：
- `config.py` 中统一处理：
  - `AI_AGENT_BASE_URL`
  - `DEEPSEEK_BASE_URL`
  - `OPENAI_BASE_URL`
- 如果检测到 DeepSeek 域名，规范为 `https://api.deepseek.com`
- 默认模型根据 base URL 自动切换

经验：
- 接 OpenAI 兼容厂商时，配置层要做归一化，不要把选择逻辑散落在业务代码里

### 2.5 “读取当前目录下的 test.txt” 只能当成整句路径

问题：
- 自然语言输入 `读取当前目录下的test.txt`
- 早期实现把整句当文件路径，导致 `File not found`

解决：
- 改进路径提取逻辑
- 优先识别 `test.txt` 这类文件名
- 再结合当前目录和工作区路径去解析

经验：
- 文件工具不能只接受纯路径，还要支持自然语言指令里的文件名

### 2.6 文件“读取了”，但没有真正“总结”

问题：
- 返回内容偏“读取预览”，总结能力不稳定
- 文档内容较长时，回答容易变成片段回显

解决：
- 新增文件内容摘要函数
- 读取后输出：
  - 路径
  - 编码
  - 行数
  - 简要摘要
  - 关键点
- 支持文本编码自动识别

经验：
- 文件工具应该拆成“读取”和“总结”两步，但最终对用户呈现要合并成简洁结论

### 2.7 终端/PowerShell 环境对路径和引号很敏感

问题：
- 带空格的目录名容易让命令解析失败
- PowerShell 在不同进程里对 PATH、profile、脚本策略的表现不一致

解决：
- 尽量使用绝对路径
- 跨目录执行时先切到项目目录
- 对命令参数谨慎引用

经验：
- Windows 环境下，命令稳定性比“写得短”更重要

### 2.8 代码中存在乱码和旧逻辑残留

问题：
- 迭代过程中出现了编码损坏的字符串
- 半成品逻辑让排错成本变高

解决：
- 直接重写 `main.py`
- 保持业务代码结构清晰
- 去掉不必要的兼容残留

经验：
- 如果局部补丁已经不可信，重写单个文件比继续打补丁更省时间

## 3. 当前可复用的最佳实践

### 3.1 后端

- 用 FastAPI 只保留必要接口
- `/chat` 请求只接收最小必要字段：
  - `message`
  - `history`
  - `session_id`
- 工具调用只保留一个核心工具起步：
  - `read_file_tool`
- 优先使用原生 Python，不引入不必要框架

### 3.2 前端

- 只保留必要布局：
  - 对话窗口
  - API 接口展示
  - 聊天输入
  - 工具调用记录
- 用原生 `fetch`
- 只维护一份 `messages` 数组

### 3.3 配置

- `.env` 统一管理：
  - `AI_AGENT_API_KEY`
  - `AI_AGENT_BASE_URL`
  - `AI_AGENT_MODEL`
  - `DEBUG`
- DeepSeek / OpenAI 切换只改配置，不改业务

### 3.4 文件读取

- 先解析自然语言中的文件名
- 再按当前目录、工作区、绝对路径依次尝试
- 读取后自动生成摘要

## 4. 下一次开发 Agent 的约束

### 4.1 代码要简洁

- 优先复用已有结构
- 不要为了“更完整”引入多余抽象
- 不写重复逻辑
- 不扩散新依赖

### 4.2 不要消耗多余 token

- 先读文件，再动手改
- 只改相关文件，不重写无关模块
- 只在必要时给出解释
- 不做长篇计划，不做重复确认

### 4.3 修改策略

- 优先小补丁
- 如果文件已经混乱，直接重写单文件
- 先验证语法，再验证接口，再验证端到端

### 4.4 排错顺序

1. 看进程和端口
2. 看配置是否加载
3. 看接口是否响应
4. 看文件解析是否正确
5. 再看模型调用

## 5. 这次项目的结论

- 后端已经支持 FastAPI REST API
- `.env` 已可读取 DeepSeek 配置
- 文件读取可以识别当前目录下的 `test.txt`
- 文件内容能够返回摘要，不只是原样读取
- 前端可以和后端稳定联动

## 6. 给下一次 Agent 的一句话要求

> 先看现有代码和配置，再做最小改动；代码保持短、直、可验证，避免重复实现和无效输出。
